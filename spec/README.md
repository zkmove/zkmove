## ZKMove Specifications

The spec describes the constraints of zkmove's execution chip(aka opcodes) and other necessary components.

We describe the constraints with Rust pseudo-code, with each module describe the constraints of the corresponding opcode.

